# `<ta-card-title>` — `CardTitleComponent`
**Quand l'utiliser** : Titre dans l'en-tête d'une carte. Voir `_composed/card.md`.
**Template canonique** :
```html
<ta-card-title>Mon titre de carte</ta-card-title>
```
**Notes** : Pas d'inputs. À placer dans `<ta-card-header>`.
