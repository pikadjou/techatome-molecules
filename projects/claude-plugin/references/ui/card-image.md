# `<ta-card-image>` — `CardImageComponent`
**Quand l'utiliser** : Image dans une carte (bannière ou vignette). Voir `_composed/card.md`.
**Template canonique** :
```html
<ta-card-image [src]="item.imageUrl" />
```
**Inputs** :
- `src` : `string` — URL de l'image (`''` par défaut)
