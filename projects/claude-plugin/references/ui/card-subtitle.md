# `<ta-card-subtitle>` — `CardSubtitleComponent`
**Quand l'utiliser** : Sous-titre dans l'en-tête d'une carte. Voir `_composed/card.md`.
**Template canonique** :
```html
<ta-card-subtitle>Sous-titre descriptif</ta-card-subtitle>
```
**Notes** : Pas d'inputs. À placer dans `<ta-card-header>`.
