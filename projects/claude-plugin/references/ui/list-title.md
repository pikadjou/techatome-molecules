# `<ta-list-title>` — `ListTitleComponent`
**Quand l'utiliser** : Titre principal d'un élément de liste. Voir `_composed/list.md`.
**Template canonique** :
```html
<ta-list-title>Jean Dupont</ta-list-title>
```
**Notes** : Pas d'inputs. Conteneur `<ng-content>` pour le texte.
