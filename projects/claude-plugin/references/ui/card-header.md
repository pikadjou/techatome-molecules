# `<ta-card-header>` — `CardHeaderComponent`
**Quand l'utiliser** : En-tête d'une carte `<ta-card>`. Voir `_composed/card.md` pour le pattern complet.
**Template canonique** :
```html
<ta-card-header>
  <ta-card-title>Mon titre</ta-card-title>
  <ta-card-subtitle>Sous-titre</ta-card-subtitle>
</ta-card-header>
```
**Notes** : Pas d'inputs. Conteneur structurel via `<ng-content>`.
