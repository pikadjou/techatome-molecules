{
  "description": "techatome code quality hooks — warns about sort-keys, missing this. and non-null assertions in templates, host class bindings, and SCSS raw values or hand-written tokens when editing Angular files",
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/techatome_conventions_hook.py"
          }
        ],
        "matcher": "Edit|Write|MultiEdit"
      }
    ]
  }
}
