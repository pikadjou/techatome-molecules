import { TaTranslationInput } from '@lib/form-input/translation.service';

describe('TaTranslationInput', () => {
  it('should be defined', () => {
    expect(TaTranslationInput).toBeDefined();
  });
});
